# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi

export GREP_OPTIONS='--color=auto'
export CLICOLOR=1

#PATH=$(npm bin):$PATH

# User specific aliases and functions


export PATH=/usr/local/share/npm/bin:$PATH
export PATH=/Users/p2744707/.nvm/versions/node/v8.10.0/bin:$PATH

alias ll='ls -l'
#alias ftree="find . -type d | sed 's/[^/]*//|_/g' | more";

alias tshape="find . -type d | sed 's/[^/]*/\|_/g'"

#MONOREPO LOCAL VARS
MONOROOT='~/Software/shared/src/app'
SHARED='~/Software/shared/src/app'
MSERV='~/Software/shared/src/app/services/'

[ -f ~/.fzf.bash ] && source ~/.fzf.bash

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
